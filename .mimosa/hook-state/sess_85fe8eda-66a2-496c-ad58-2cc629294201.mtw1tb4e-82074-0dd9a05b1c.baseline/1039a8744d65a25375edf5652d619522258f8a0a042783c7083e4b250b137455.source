/**
 * `whale-tasks` namespace dictionaries: the task-board row copy.
 */
/** Dictionary namespace owned by this plugin. */
export declare const NS = "whale-tasks";
/** Simplified Chinese dictionary (the key-set source of truth). */
export declare const zh: {
    'board.title': string;
    'board.empty': string;
    'board.runAt': string;
    'board.lastRun': string;
    'board.none': string;
    'board.overdue': string;
    'board.missed': string;
    'status.active': string;
    'status.paused': string;
    'status.done': string;
};
/** English dictionary (same key set). */
export declare const en: Record<WhaleTaskKey, string>;
/** Union of this namespace's dictionary keys. */
export type WhaleTaskKey = keyof typeof zh;
//# sourceMappingURL=locales.d.ts.map