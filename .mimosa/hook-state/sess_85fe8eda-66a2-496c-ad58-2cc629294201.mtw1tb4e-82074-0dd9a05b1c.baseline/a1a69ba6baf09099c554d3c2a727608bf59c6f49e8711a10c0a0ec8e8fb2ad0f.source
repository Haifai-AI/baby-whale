import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
/**
 * The workbook preview card: Claude-style tabs over one xlsx payload —
 * `Data` (the native grid studio), `Charts (n)` (the workbook's embedded
 * charts re-rendered as themed SVG), and `Original` (LibreOffice's
 * single-page-per-sheet render of the real file). Each tab degrades away
 * independently when its source is unavailable.
 * @module @deepseek-ai/dsh-client-ui-whale-artifacts/src/client/WorkbookPreview
 */
import { useState } from 'react';
import { ArtifactStudioBody } from '@deepseek-ai/dsh-client-ui-whale-artifact/client';
import { WorkbookCharts } from "./WorkbookCharts.js";
import css from './ArtifactsView.module.css';
/** Small stroked tab icons, sized to the 16px tab row. */
function TabIcon({ kind }) {
    const common = { width: 14, height: 14, viewBox: '0 0 16 16', fill: 'none', 'aria-hidden': true };
    if (kind === 'data') {
        return (_jsxs("svg", { ...common, stroke: "currentColor", strokeWidth: 1.4, children: [_jsx("ellipse", { cx: "8", cy: "3.6", rx: "5.4", ry: "2.2" }), _jsx("path", { d: "M2.6 3.6v8.8c0 1.2 2.4 2.2 5.4 2.2s5.4-1 5.4-2.2V3.6" }), _jsx("path", { d: "M2.6 8c0 1.2 2.4 2.2 5.4 2.2s5.4-1 5.4-2.2" })] }));
    }
    if (kind === 'charts') {
        return (_jsxs("svg", { ...common, stroke: "currentColor", strokeWidth: 1.6, strokeLinecap: "round", children: [_jsx("path", { d: "M3 13.5V9" }), _jsx("path", { d: "M8 13.5v-11" }), _jsx("path", { d: "M13 13.5V6" })] }));
    }
    return (_jsxs("svg", { ...common, stroke: "currentColor", strokeWidth: 1.4, strokeLinejoin: "round", children: [_jsx("path", { d: "M4 1.8h5.2L13 5.6v8.6H4z" }), _jsx("path", { d: "M9.2 1.8v3.8H13" })] }));
}
/**
 * The tabbed workbook lens. Remounted (keyed) per selected file so the tab
 * resets to Data.
 */
export function WorkbookPreview({ data, t }) {
    const [tab, setTab] = useState('data');
    const chartCount = data.charts?.length ?? 0;
    const hasOriginal = typeof data.pdfPath === 'string' && data.pdfPath.length > 0;
    const tabButton = (kind, label, badge) => (_jsxs("button", { type: "button", className: `${css.wbTab} ${tab === kind ? css.wbTabActive : ''}`, onClick: () => { setTab(kind); }, children: [_jsx(TabIcon, { kind: kind }), label, badge !== undefined && _jsx("span", { className: css.wbBadge, children: badge })] }));
    return (_jsxs("div", { className: css.workbook, "data-whale-workbook": "", children: [_jsxs("div", { className: css.wbTabs, children: [tabButton('data', t('tab.data')), chartCount > 0 && tabButton('charts', t('tab.charts'), chartCount), hasOriginal && tabButton('original', t('tab.original'))] }), _jsxs("div", { className: css.wbBody, children: [tab === 'data' && _jsx(ArtifactStudioBody, { preview: data }), tab === 'charts' && _jsx(WorkbookCharts, { charts: data.charts ?? [], allLabel: t('charts.all') }), tab === 'original' && hasOriginal && (_jsx("iframe", { title: data.file_name, src: `/api/artifacts.file?path=${encodeURIComponent(data.pdfPath ?? '')}`, className: css.pdfFrame }))] })] }));
}
//# sourceMappingURL=WorkbookPreview.js.map