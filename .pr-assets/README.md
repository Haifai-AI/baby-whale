# PR screenshot assets

Before/after captures for `polish/apple-ui`. Each image is a side-by-side of the
same surface rendered from `origin/main` (left, BEFORE) and from the branch
(right, AFTER) in a Chromium window at 1440x900, device scale 2.

Delete this branch once the pull request has been reviewed.
